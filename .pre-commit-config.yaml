repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v4.5.0
  hooks:
    - id: check-yaml
    - id: end-of-file-fixer
    - id: trailing-whitespace
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.2.2
  hooks:
    # Run the linter.
    - id: ruff
      args: [ --fix ]
    # Run the formatter.
    - id: ruff-format
- repo: https://github.com/python-poetry/poetry
  rev: '1.8.2'  # add version here
  hooks:
    - id: poetry-check
    - id: poetry-lock
    - id: poetry-export
      args: ["--without-hashes", "-f", "requirements.txt", "-o", "./requirements.txt"]
    - id: poetry-install
# - repo: local
#   hooks:
#     - id: django-generate-swagger
#       name: django-generate-swagger
#       entry: python manage.py generate_swagger -o swagger.json
#       always_run: true
#       language: system
#       pass_filenames: false
